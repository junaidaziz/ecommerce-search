/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.shopify.com',
      'images.unsplash.com',
      'source.unsplash.com',
      'picsum.photos',
    ],
  },
};

module.exports = nextConfig;
